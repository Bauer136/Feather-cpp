# Feather Lang (CPP Compiler)

Feather is a Hobby programming language.

## Building

Requires `nasm` and `ld` on a Linux operating system.

```bash
git clone 
cd hydrogen-cpp
mkdir build
cmake -S . -B build
cmake --build build
```

Executable will be `feather` in the `build/` directory.
